export const minimal = ({ id, username }) => ({
  id,
  username,
});

export const show = ({
  id,
  email,
  username,
  firstName,
  lastName,
}) => ({
  id,
  email,
  username,
  firstName,
  lastName,
});
